# 小怪獸售票機 V7.4 Enterprise

這是由 V7.3 Phase 3F Part 2 GitHub Pages FIX 直接升級的完整版本。

## 部署

1. 將 ZIP 解壓。
2. 將全部檔案覆蓋 GitHub 儲存庫根目錄。
3. 確認 `.nojekyll` 與 `index.html` 同層。
4. GitHub Pages 使用 `main` + `/(root)`。
5. 部署後按 Ctrl + F5，點餐機則關閉再重新開啟。

## 版本確認

頁面右下角顯示 `V7.4.0 · ONLINE` 即代表新核心已載入。
