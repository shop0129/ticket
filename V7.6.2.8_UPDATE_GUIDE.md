# V7.6.2.8 Ticket Order Bottom & Staff Sync Fix

## 覆蓋檔案
- js/modules/ticketManager.js
- js/staff/enterprise-manager.js
- staff.html
- service-worker.js
- CHANGELOG.md

## 更新內容
1. 售票機票券管理新增「移至最下面」。
2. 手機員工後台票券管理新增「移至最下面」。
3. 手機員工後台改為依 `sortOrder` 排序，不再依 Firebase 物件鍵值順序顯示。
4. 手機端上移、下移、移至最下面後，儲存時會重新編號 `sortOrder` 並同步 Firebase。
5. 新增票券與複製票券會自動排到最下面。
6. 更新 PWA 快取版本。

## 部署
直接覆蓋檔案後重新部署 Firebase Hosting，不需修改 Rules 或 Functions。
手機請完全關閉舊分頁後重新開啟；若仍為舊版，清除網站資料或移除後重新加入主畫面捷徑。
