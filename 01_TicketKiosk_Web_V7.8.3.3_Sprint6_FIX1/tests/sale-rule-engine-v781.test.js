const fs=require('fs'),vm=require('vm'),assert=require('assert');const c={window:{salesHistory:[]},setInterval:()=>0};c.window.window=c.window;vm.createContext(c);vm.runInContext(fs.readFileSync('js/core/sale-rule-engine.js','utf8'),c);const E=c.window.MonsterSaleRule;let t={enable:true,saleRule:{enabled:true,startTime:'10:00',endTime:'11:30',weekdays:[],dailyLimit:0}};assert.equal(E.evaluate('x',t,{now:new Date(2026,6,20,10,30)}).available,true);assert.equal(E.evaluate('x',t,{now:new Date(2026,6,20,11,31)}).reason,'after_time');console.log('V7.8.1 tests passed');