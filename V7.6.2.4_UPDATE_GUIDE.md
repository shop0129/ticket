# V7.6.2.4 Order Lookup Sync Fix

## 修正
- 快速查詢與 QR 掃描改用與訂單中心相同的訂單資料來源。
- 查不到時會先向 Firebase 強制重新同步一次，再顯示找不到。
- 支援 orderNo、shortCode、queueNumber、Firebase key 與收據短單號。
- 舊版 QR 沒有 token 時不再被錯誤擋下。
- 新版 QR 仍會驗證 token。

## 部署
覆蓋檔案後重新部署 Firebase Hosting，並關閉手機舊分頁再重開。無需更新 Rules 或 Functions。
