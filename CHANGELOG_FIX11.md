# V7.8.3.3 FIX11

## 修正

- 現金付款框不再只依賴外部 CSS 顯示，避免點餐機讀到舊 PWA 樣式時整個付款資訊框消失。
- 選擇現金付款後立即顯示應付、已投入、尚差與票券數量。
- 收款途中即時顯示紙鈔張數、硬幣枚數及各面額投入明細。
- 付款框固定覆蓋在目前購票／確認票券頁，不會跑到首頁。
- 收據列印進度改用 CSS 平滑動畫與 `requestAnimationFrame` 更新百分比。
- 實體收據較快完成時仍保留最短可見動畫，不再從 0% 直接跳至 94%。
- 控制器確認 `PRINTED` 前仍以 94% 為安全上限，只有實體列印成功才顯示 100%。

## 保留

- Controller 113 的安全取消流程。
- 100／500／1000 元紙鈔與低價票找零策略。
- 自動找零完成後才列印收據。
- 店長／員工 QR 掃描、頂部安全距離與角色權限。
