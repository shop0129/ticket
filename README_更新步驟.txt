小怪獸售票機 V7.8.3.3 FIX10
售票頁頂部「返回／請選擇票種」顯示修正

更新方式：

1. 將本資料夾內的 index.html、service-worker.js、css 資料夾，
   依照相同路徑覆蓋到目前 ticket GitHub Pages 專案。
2. 等待 GitHub Pages 部署完成。
3. 完全關閉點餐機售票頁，再重新開啟。
4. 右下角應顯示：
   V7.8.3.3 · FIX10 HEADER + CASH CANCEL

修正內容：

- 返回按鈕與「請選擇票種」保留至少 20px 的頂部安全距離。
- Header 固定高度且禁止被直向 Flex 版面壓縮。
- 捲動票券列表時 Header 保持在畫面頂端。
- 同步保護票券明細頁的返回按鈕。
- 現金付款從收據機檢查開始就立即顯示「應付／已付／尚差」。
- 尚未投入任何現金時，可按「取消付款並返回」。
- 已投入任何現金後禁止直接離開，避免帳款與訂單不同步。

版本需求：

- 必須更新 Controller 113 才能使用網頁安全取消。
- 保留原有收鈔、找零、收據與票券資料流程。
