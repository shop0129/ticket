# V7.8.3.3 FIX26G — 公休日保存修正

- 修正每週固定公休日存檔後又恢復星期一。
- 設定版本改為單調遞增，不受點餐機與其他裝置的時鐘差異影響。
- 公休日增加穩定字串格式與本機備援，支援任意星期、複選及空白設定。
- Firebase 寫入後回讀核對，只有實際公休日一致才顯示同步成功。
- 移除舊版 `saveBusinessMode()` 同名函式，避免誤存到已停用的 `businessData`。
- 保留 FIX26F LINE Pay 正式收款、Kiosk 125 與 Controller 113 行為。
